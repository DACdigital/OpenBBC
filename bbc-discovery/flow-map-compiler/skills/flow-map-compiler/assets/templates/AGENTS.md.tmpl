---
schema_version: 2
generated_by: flow-map-compiler
generated_at: {{generated_at_iso}}
generated_from_sha: {{generated_from_sha}}
app_name: {{app_name}}
stack:
  framework: {{framework_adapter}}
  version: "{{framework_version}}"
  router: {{router}}
  language: {{language}}
counts:
  skills: {{counts_skills}}
  flows: {{counts_flows}}
  endpoints: {{counts_endpoints}}
freshness:
  last_verified: {{generated_at_iso}}
  staleness_check: weekly
files:
  app_context: APP.md
  glossary: glossary.md
  skills_dir: skills/
  flows_dir: flows/
  endpoints_dir: endpoints/
---

# {{app_name}} — flow map

<!-- AGENT id="summary" -->
{{app_summary_one_paragraph}}
<!-- /AGENT -->

## Reading order for agents

1. Load APP.md once per session.
2. For "I want to do X" → load `skills/<id>.md` (primary read — domain context).
3. For "what triggered this UI" → load `flows/<id>.md`.
4. For "what's the HTTP shape of call Y" → load `endpoints/<id>.md`.
5. `glossary.md` is the one-page pivot, not a primary read.

## Overview

```mermaid
flowchart LR
{{overview_flowchart_body_indented_2}}
```

## Skills

| skill | file | suggests N endpoints |
|---|---|---|
{{#each skills}}
| {{this.id}} | [skills/{{this.id}}.md](skills/{{this.id}}.md) | {{this.suggested_count}} |
{{/each}}

## Flows

| id | file | what it does |
|---|---|---|
{{#each flows}}
| {{this.id}} | [flows/{{this.id}}.md](flows/{{this.id}}.md) | {{this.summary}} |
{{/each}}

## Endpoints

| id | method | path | used by skills |
|---|---|---|---|
{{#each endpoints}}
| `{{this.id}}` | {{this.method}} | `{{this.path}}` | {{this.used_by_skills_csv_or_dash}} |
{{/each}}

## Note on endpoint and tool naming

Endpoint ids referenced throughout this wiki are **proposed** — derived from
frontend call sites. They become MCP tool names downstream; the bundle and the
runtime agent refer to them as *tools*. Inside `.flow-map/`, they are always
*endpoints*.

## Unresolved

{{unresolved_summary}}

<!-- HUMAN id="agents-extra" -->
<!-- /HUMAN -->
