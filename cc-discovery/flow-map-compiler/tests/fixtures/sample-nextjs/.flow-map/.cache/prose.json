{
  "AGENTS": {
    "summary": "A minimal Next.js App Router application with one user-facing flow: profile editing. The agent surface is one screen at /profile and one write capability against the user record."
  },
  "APP": {
    "overview": "sample-nextjs is a single-screen Next.js App Router application used as a flow-map vertical-slice fixture. It exists to validate that the wiki shape is framework-invariant -- a parallel sample-react fixture exposes the identical user flow against the identical backend contract.",
    "invariants_md": "1. The acting user's id is sourced client-side from `localStorage.userId`.\n2. All authenticated requests carry a bearer token from `localStorage.token`.\n3. All API paths are mounted under `/api` and called as relative URLs.\n4. Error responses are surfaced verbatim to the user.",
    "auth_model": "The user signs in elsewhere; the bearer token and user id are persisted in `localStorage`. Each request reads both at invocation time. There is no automatic refresh -- a 401 response is surfaced as `please sign in again`.",
    "conventions_md": "- Form submission is wrapped in a `try / finally` that flips a `saving` flag.\n- Errors set local component state and render below the form.\n- One API helper per resource lives under `lib/api/`.",
    "boundaries_md": "1. The agent must never attempt to mutate another user's record.\n2. The agent must never invent fields not declared in the relevant capability's body shape.\n3. The agent must never read or transmit the bearer token in agent output."
  },
  "flows": {
    "update-profile": {
      "name": "Update profile",
      "description": "Use when the user wants to change their account name or email",
      "intent": "Persist the user's edited profile fields to the backend",
      "user_phrases": ["change my name", "update my email", "fix my profile", "my email is wrong"],
      "trigger": "user submits the profile form",
      "preconditions": [
        "User is signed in (a bearer token is present in client-side storage)",
        "The form has at least one changed field"
      ],
      "postconditions": [
        "The user's profile reflects the submitted values",
        "Subsequent reads of the user record return the new values"
      ],
      "side_effects": ["audit-log-entry"],
      "related_flows": [],
      "confidence": "high",
      "prose": "The user opens their profile page, edits one or both of name and email, and submits. The agent's job is to take the user-stated change, validate that the user is authenticated, and perform the write intent registered in the glossary. The flow is single-step and idempotent -- re-running with the same values is safe.",
      "how_reached": "reached when the user navigates to `/profile` in the browser",
      "intents_as_phrase": {
        "update-user-record": "update user record"
      },
      "steps": [
        "Confirm the user is signed in. If not, ask them to sign in first and stop.",
        "Confirm which field(s) the user wants to change and the new value(s).",
        "Perform [update user record](../glossary.md#update-user-record) with the user's id and the changed fields.",
        "On success, summarise what was changed back to the user.",
        "On failure, show the error returned by the tool and offer to retry once."
      ],
      "decision_points": [
        { "condition": "User did not provide a new value", "action": "ask for it before performing the write intent" },
        { "condition": "User asked to change a field the intent does not accept", "action": "tell the user that field cannot be changed here" },
        { "condition": "Two retries failed", "action": "stop retrying and ask the user whether to surface a support request" }
      ],
      "failure_modes": [
        { "what": "Tool returns 401", "means": "Bearer token missing or expired", "do": "Ask the user to sign in again" },
        { "what": "Tool returns 403", "means": "User trying to update someone else", "do": "Refuse and explain the boundary" },
        { "what": "Tool returns 422", "means": "Validation failed (e.g., bad email)", "do": "Echo the error and ask for a fix" },
        { "what": "Tool returns 5xx", "means": "Backend transient failure", "do": "Retry once with backoff, then surface" }
      ]
    }
  },
  "capabilities": {
    "users": {
      "summary": "Read and modify the authenticated user's account record",
      "overview": "The `users` capability covers operations on a single user account record. The runtime agent should treat user records as tenant-scoped: a user can only modify their own record, and the bearer token determines the acting identity. Field-level permissions are enforced server-side, so the agent does not need to filter inputs -- but it should refuse on behalf of the user when the user asks to change a field that this capability cannot change.",
      "concepts": [
        "The `id` path parameter is always the acting user's own id, sourced client-side from the session.",
        "All writes are authenticated with a bearer token; missing or expired tokens produce 401.",
        "The backend currently exposes only one verb on this capability: `update`. There is no create or delete from this surface."
      ],
      "when_to_reach": [
        { "intent": "change my name", "tool": "users.update" },
        { "intent": "fix my email", "tool": "users.update" },
        { "intent": "update my profile", "tool": "users.update" }
      ],
      "tool_overrides": {
        "users.update": {
          "does": "Update the authenticated user's name and/or email",
          "when_to_call_phrases": [
            "change my name to X",
            "update my email to Y",
            "fix my profile"
          ]
        }
      },
      "cannot_do": [
        "Cannot delete a user account (no tool surface).",
        "Cannot change another user's record (server enforces 403).",
        "Cannot rotate a password (separate capability, not yet documented)."
      ]
    }
  }
}
